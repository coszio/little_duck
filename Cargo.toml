[package]
name = "little_duck"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[build-dependencies]
lalrpop = "0.19.7"

[dependencies]
lalrpop-util = {features = ['lexer'], version = "0.19.7"}
regex = "1"